# GPU高速化設定
# GPU処理を有効にする場合は true、無効にする場合は false
USE_GPU=true

# 注意:
# - GPU処理を有効にするには CuPy のインストールが必要です
# - USE_GPU=true でも CuPy がインストールされていない場合は自動的に CPU 処理になります
# - USE_GPU=false にすると、CuPy がインストールされていても CPU 処理を強制します
